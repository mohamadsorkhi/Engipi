# سند طراحی UX/UI ویزارد ثبت مهارت (Skill Select)

## 1. هدف و محدوده

هدف، بازطراحی تجربه انتخاب مهارت متخصص به یک Wizard پنج‌مرحله‌ای است، بدون تغییر در Backend، Database یا قرارداد ثبت فعلی.

مراحل پیشنهادی:

1. انتخاب حوزه
2. انتخاب گرایش
3. انتخاب مهارت‌ها
4. تعیین سطح و سابقه
5. بازبینی و ثبت

این سند فقط مشخصات UX/UI را تعریف می‌کند. هیچ Route، Controller، Model، JavaScript، Blade، API، payload یا validation سمت سرور در این طراحی تغییر نمی‌کند.

اصول اصلی طراحی:

- در هر مرحله فقط یک تصمیم اصلی از کاربر خواسته شود.
- داده‌های انتخاب‌شده در تمام مراحل در همان state فعلی صفحه باقی بمانند.
- Back فقط مرحله نمایشی را عوض کند و هیچ داده‌ای را پاک نکند.
- ثبت اطلاعات فقط در مرحله پنجم انجام شود.
- محدودیت‌های فعلی حفظ شوند: حداکثر ۲ حوزه، ۲ گرایش و ۵ مهارت.
- جداسازی مهارت نرم‌افزاری و میدانی بر اساس `skill_type` حفظ شود.
- Process در UI نمایش یا انتخاب داده نشود؛ این داده همچنان در Backend مشتق می‌شود.

---

## 2. ساختار کلی صفحه

صفحه از پنج ناحیه اصلی تشکیل شود:

1. عنوان و توضیح کوتاه صفحه
2. Progress Stepper
3. محتوای مرحله فعال
4. خلاصه کوچک انتخاب‌ها
5. نوار ثابت یا نیمه‌ثابت دکمه‌های Back و Next

چیدمان پیشنهادی دسکتاپ:

```text
┌──────────────────────────────────────────────────────────────────────┐
│ انتخاب مهارت‌های تخصصی                                               │
│ مهارت‌هایی را انتخاب کنید که تجربه واقعی انجام آن‌ها را دارید.       │
├──────────────────────────────────────────────────────────────────────┤
│  ① حوزه ─── ② گرایش ─── ③ مهارت‌ها ─── ④ سطح و سابقه ─── ⑤ بازبینی  │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│                       محتوای مرحله فعال                              │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│ خلاصه: ۲ حوزه · ۱ گرایش · ۳ مهارت                                   │
├──────────────────────────────────────────────────────────────────────┤
│ [بازگشت]                                           [ادامه ←]         │
└──────────────────────────────────────────────────────────────────────┘
```

در دسکتاپ، عرض مفید محتوا بین ۸۰۰ تا ۱۱۰۰ پیکسل باشد. در صفحه‌های عریض، محتوا در مرکز قرار گیرد و خطوط متنی بیش از حد کشیده نشوند.

---

## 3. Progress Stepper

### 3.1 ساختار

Stepper شامل پنج مرحله شماره‌دار است:

```text
① انتخاب حوزه
② انتخاب گرایش
③ انتخاب مهارت‌ها
④ سطح و سابقه
⑤ بازبینی
```

### 3.2 وضعیت‌های بصری

هر Step یکی از وضعیت‌های زیر را دارد:

- **فعال:** دایره پررنگ، عنوان برجسته و خط اتصال فعال تا مرحله قبل
- **تکمیل‌شده:** آیکون تیک به‌جای شماره و رنگ موفقیت
- **آینده:** شماره و عنوان خنثی
- **دارای خطا:** حاشیه یا نشان خطا و عنوان با رنگ هشدار
- **نیازمند بازبینی:** زمانی که تغییر مرحله قبلی داده مرحله بعد را حذف یا نامعتبر کرده است

### 3.3 رفتار کلیک روی Stepper

- کلیک روی مرحله‌های تکمیل‌شده مجاز باشد و مانند Back عمل کند.
- کلیک مستقیم روی مرحله‌های آینده مجاز نباشد.
- پس از یک بار رسیدن معتبر به مرحله‌ای، بازگشت به آن از Stepper مجاز است؛ اما عبور دوباره از مراحل نامعتبر انجام نشود.
- Stepper نباید خودش Submit ایجاد کند.

### 3.4 Stepper دسکتاپ

- نمایش افقی کامل
- عنوان هر پنج مرحله قابل مشاهده
- خط اتصال بین مراحل
- شماره یا تیک با اندازه حداقل ۳۲ پیکسل

### 3.5 Stepper موبایل

نسخه فشرده:

```text
مرحله ۳ از ۵
انتخاب مهارت‌ها
[████████████░░░░░░░░]
```

در موبایل به‌جای فشرده‌کردن پنج عنوان کنار هم، فقط مرحله فعال، شمارنده و Progress Bar نمایش داده شود. در زیر آن می‌توان یک ردیف پنج‌نقطه‌ای کوچک برای درک موقعیت کلی قرار داد.

---

## 4. مرحله اول: انتخاب حوزه

### هدف

انتخاب یک یا دو حوزه تخصصی اصلی.

### محتوا

- عنوان: «حوزه‌های تخصصی خود را انتخاب کنید»
- متن راهنما: «حداکثر دو حوزه‌ای را انتخاب کنید که بیشترین ارتباط را با تجربه شما دارند.»
- dropdown یا فهرست جست‌وجوپذیر حوزه‌ها
- chip حوزه‌های انتخاب‌شده
- شمارنده `۰ از ۲` تا `۲ از ۲`

### رفتار انتخاب

- انتخاب هر حوزه، آن را به شکل chip در بخش «انتخاب‌شده‌ها» نمایش دهد.
- انتخاب مجدد یک حوزه تغییری ایجاد نکند و پیام کوتاه «این حوزه قبلاً انتخاب شده است» نشان داده شود.
- پس از انتخاب دو حوزه، گزینه‌های انتخاب‌نشده همچنان قابل مشاهده اما غیرفعال باشند.
- هر chip دکمه حذف واضح داشته باشد.

### Validation مرحله

- حداقل ۱ حوزه الزامی است.
- حداکثر ۲ حوزه مجاز است.
- هر حوزه باید از گزینه‌های موجود صفحه انتخاب شده باشد.

### وضعیت دکمه‌ها

- Back در مرحله اول نمایش داده نشود یا به‌صورت غیرفعال باشد.
- Next تا قبل از انتخاب حداقل یک حوزه Disabled باشد.
- پس از انتخاب یک یا دو حوزه، Next فعال شود.
- با رسیدن به سقف دو حوزه، خود dropdown برای انتخاب گزینه سوم محدود شود؛ Next همچنان فعال بماند.

### پیام‌های پیشنهادی

- حالت خالی: «برای ادامه حداقل یک حوزه انتخاب کنید.»
- سقف انتخاب: «حداکثر دو حوزه قابل انتخاب است.»
- حذف حوزه دارای داده وابسته: پیام تأیید لازم است.

### Wireframe مرحله اول

```text
┌──────────────────────────────────────────────────────────────┐
│ ① حوزه   ② گرایش   ③ مهارت‌ها   ④ سطح و سابقه   ⑤ بازبینی  │
├──────────────────────────────────────────────────────────────┤
│ حوزه‌های تخصصی خود را انتخاب کنید                            │
│ حداکثر دو حوزه‌ای را انتخاب کنید که تجربه بیشتری در آن دارید.│
│                                                              │
│ حوزه تخصصی                                                   │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ جست‌وجو یا انتخاب حوزه                              ▾     │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ انتخاب‌شده‌ها                                      ۱ از ۲    │
│ [ مهندسی عمران × ]                                           │
│                                                              │
│ راهنما: انتخاب حوزه، گرایش‌های مرتبط را در مرحله بعد فعال می‌کند.│
├──────────────────────────────────────────────────────────────┤
│                                                   [ادامه ←]  │
└──────────────────────────────────────────────────────────────┘
```

---

## 5. مرحله دوم: انتخاب گرایش

### هدف

انتخاب یک یا دو گرایش از زیرمجموعه حوزه‌های مرحله اول.

### محتوا

- عنوان: «گرایش‌های مرتبط را انتخاب کنید»
- نمایش حوزه‌های انتخاب‌شده به‌صورت فیلتر یا عنوان گروه
- dropdown یا فهرست Subdomainهای متعلق به حوزه‌های انتخاب‌شده
- chip گرایش‌های انتخاب‌شده
- شمارنده `۰ از ۲` تا `۲ از ۲`

### طراحی فهرست

اگر دو حوزه انتخاب شده باشد، گرایش‌ها به تفکیک حوزه گروه‌بندی شوند:

```text
مهندسی عمران
  سازه
  مدیریت ساخت

مهندسی مکانیک
  طراحی کاربردی
  ساخت و تولید
```

نام حوزه والد در هر گزینه یا گروه مشخص باشد تا گرایش‌های هم‌نام قابل تشخیص باشند.

### Validation مرحله

- حداقل ۱ گرایش الزامی است.
- حداکثر ۲ گرایش مجاز است.
- هر گرایش باید متعلق به یکی از حوزه‌های انتخاب‌شده باشد.
- گرایش تکراری پذیرفته نشود.

### وضعیت دکمه‌ها

- Back همیشه فعال است.
- Next تا انتخاب حداقل یک گرایش Disabled است.
- در سقف دو گرایش، انتخاب گزینه سوم Disabled شود.

### رفتار تغییر حوزه قبلی

اگر کاربر به مرحله اول برگردد و حوزه‌ای را حذف کند:

- اگر هیچ گرایش یا مهارت وابسته‌ای وجود ندارد، حذف مستقیم انجام شود.
- اگر گرایش یا مهارت وابسته وجود دارد، قبل از حذف Dialog تأیید نمایش داده شود.
- متن Dialog مشخص کند که حذف حوزه، گرایش‌ها و مهارت‌های وابسته را نیز از انتخاب خارج می‌کند.
- با انصراف، state بدون تغییر بماند.
- با تأیید، داده‌های وابسته طبق رفتار فعلی پاک شوند و مرحله‌های بعدی «نیازمند بازبینی» شوند.

### حالت بدون داده

اگر حوزه‌ای Subdomain نداشته باشد:

- Empty State داخل همان گروه نمایش داده شود.
- متن: «برای این حوزه هنوز گرایشی تعریف نشده است.»
- کاربر بتواند Back بزند و حوزه دیگری انتخاب کند.

### Wireframe مرحله دوم

```text
┌──────────────────────────────────────────────────────────────┐
│ ✓ حوزه   ② گرایش   ③ مهارت‌ها   ④ سطح و سابقه   ⑤ بازبینی  │
├──────────────────────────────────────────────────────────────┤
│ گرایش‌های مرتبط را انتخاب کنید                              │
│ گرایش‌ها بر اساس حوزه‌های انتخاب‌شده نمایش داده می‌شوند.    │
│                                                              │
│ حوزه‌ها: [مهندسی عمران] [مهندسی مکانیک]                    │
│                                                              │
│ انتخاب گرایش                                                 │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ جست‌وجو یا انتخاب گرایش                            ▾     │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ گرایش‌های انتخاب‌شده                              ۲ از ۲    │
│ [ سازه · مهندسی عمران × ] [ طراحی کاربردی · مکانیک × ]      │
├──────────────────────────────────────────────────────────────┤
│ [→ بازگشت]                                      [ادامه ←]   │
└──────────────────────────────────────────────────────────────┘
```

---

## 6. مرحله سوم: انتخاب مهارت‌ها

### هدف

انتخاب حداکثر پنج مهارت از گرایش‌های انتخاب‌شده، بدون تعیین نهایی سطح و سابقه در این مرحله.

### ساختار محتوا

مرحله شامل این بخش‌ها باشد:

1. عنوان و شمارنده انتخاب
2. گرایش‌های انتخاب‌شده به‌صورت فیلتر
3. جست‌وجوی مهارت
4. تب یا بخش مجزای مهارت‌های نرم‌افزاری/پردازشی
5. تب یا بخش مجزای مهارت‌های میدانی
6. خلاصه مهارت‌های انتخاب‌شده
7. CTA پیشنهاد مهارت جدید

### دسته‌بندی

دو گروه فعلی حفظ شوند:

- «پردازش‌ها / مهارت‌های نرم‌افزاری»
- «مهارت‌های میدانی»

در موبایل این دو گروه به‌صورت Tab یا Accordion نمایش داده شوند. در دسکتاپ می‌توان آن‌ها را زیر هم یا در دو ستون مستقل نمایش داد، مشروط به اینکه تعداد کارت‌ها و فضای خواندن مناسب باشد.

### کارت مهارت

هر کارت شامل:

- نام مهارت
- نوع مهارت
- نام گرایش مرتبط
- حالت انتخاب‌شده/انتخاب‌نشده
- دکمه یا ناحیه «انتخاب»

سطح و سابقه در این مرحله درخواست نشود. پس از انتخاب مهارت، کارت فقط به حالت Selected برود و در مرحله چهارم تنظیمات آن کامل شود.

### جست‌وجو

- جست‌وجو فقط کارت‌های بارگذاری‌شده را فیلتر کند.
- پاک‌کردن جست‌وجو همه کارت‌های موجود را برگرداند.
- انتخاب‌های کاربر هنگام فیلتر یا تغییر Tab حفظ شوند.
- Empty State جست‌وجو: «مهارتی با این عبارت پیدا نشد.»

### محل دکمه پیشنهاد مهارت جدید

دکمه «مهارت موردنظر من در فهرست نیست» در انتهای فهرست مهارت‌های میدانی و پس از Empty State قرار گیرد.

در دسکتاپ:

- یک کارت CTA تمام‌عرض با آیکون لامپ
- متن توضیحی کوتاه
- دکمه «پیشنهاد مهارت جدید»

در موبایل:

- کارت فشرده زیر لیست
- دکمه تمام‌عرض

این CTA باید Modal مستقل فعلی را باز کند و بخشی از Next یا Submit اصلی Wizard نباشد. ثبت پیشنهاد باعث انتخاب خودکار آن به‌عنوان مهارت نشود؛ چون پیشنهاد هنوز نیازمند بررسی مدیر است.

### Validation مرحله

- حداقل ۱ مهارت الزامی است.
- حداکثر ۵ مهارت مجاز است.
- مهارت تکراری پذیرفته نشود.
- مهارت باید از Subdomainهای انتخاب‌شده بارگذاری شده باشد.

### وضعیت دکمه‌ها

- Back فعال است.
- Next تا انتخاب حداقل یک مهارت Disabled است.
- پس از انتخاب پنج مهارت، کارت‌های انتخاب‌نشده Disabled شوند.
- حذف یک مهارت، ظرفیت انتخاب را دوباره آزاد کند.

### Wireframe مرحله سوم

```text
┌────────────────────────────────────────────────────────────────────┐
│ ✓ حوزه   ✓ گرایش   ③ مهارت‌ها   ④ سطح و سابقه   ⑤ بازبینی       │
├────────────────────────────────────────────────────────────────────┤
│ مهارت‌های خود را انتخاب کنید                          ۳ از ۵       │
│ [همه گرایش‌ها] [سازه] [مدیریت ساخت]                              │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ جست‌وجوی مهارت...                                         🔍  │ │
│ └────────────────────────────────────────────────────────────────┘ │
│                                                                    │
│ پردازش‌ها / مهارت‌های نرم‌افزاری                                 │
│ ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐   │
│ │ Revit          ✓ │ │ ETABS            │ │ AutoCAD        ✓ │   │
│ │ نرم‌افزاری       │ │ نرم‌افزاری       │ │ نرم‌افزاری       │   │
│ └──────────────────┘ └──────────────────┘ └──────────────────┘   │
│                                                                    │
│ مهارت‌های میدانی                                                   │
│ ┌──────────────────┐ ┌──────────────────┐                         │
│ │ نظارت کارگاهی  ✓ │ │ متره و برآورد    │                         │
│ └──────────────────┘ └──────────────────┘                         │
│                                                                    │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ مهارت موردنظر شما در فهرست نیست؟ [پیشنهاد مهارت جدید]         │ │
│ └────────────────────────────────────────────────────────────────┘ │
├────────────────────────────────────────────────────────────────────┤
│ [→ بازگشت]                                            [ادامه ←]   │
└────────────────────────────────────────────────────────────────────┘
```

---

## 7. مرحله چهارم: تعیین سطح و سابقه

### هدف

تکمیل سطح مهارت و سال‌های تجربه برای هر مهارت انتخاب‌شده.

### ساختار

هر مهارت در یک کارت مستقل نمایش داده شود:

- نام مهارت
- نوع و گرایش مرتبط
- انتخاب سطح
- انتخاب سال سابقه
- امکان حذف مهارت

### سطح مهارت

مقادیر نمایشی و valueهای فعلی بدون تغییر حفظ شوند:

- مبتدی
- متوسط
- حرفه‌ای

برای دسکتاپ، سطح می‌تواند به‌صورت Segmented Control سه‌گزینه‌ای نمایش داده شود. در موبایل، Radio Card یا select بومی خواناتر است.

### سابقه

- کنترل انتخاب سال با برچسب واضح «سال تجربه»
- دامنه UI فعلی ۱ تا ۳۰ سال حفظ شود.
- از مقدار پیش‌فرض مبهم پرهیز شود؛ اگر مقدار پیش‌فرض از قبل به کارت داده می‌شود، به‌صورت واضح انتخاب‌شده نمایش داده شود.

### Validation مرحله

- همه مهارت‌ها باید level داشته باشند.
- همه مهارت‌ها باید years معتبر داشته باشند.
- تعداد مهارت‌ها همچنان باید بین ۱ و ۵ باشد.
- skill ID و داده‌های اصلی مهارت در UI قابل ویرایش مستقیم نباشند.

### وضعیت دکمه‌ها

- Back فعال است.
- Next تا تکمیل سطح و سابقه همه کارت‌ها Disabled باشد.
- اگر یک کارت ناقص است، همان کارت با حاشیه هشدار و پیام محلی مشخص شود.
- کلیک روی Next در حالت ناقص، در صورت فعال‌بودن اشتباه، باید به اولین کارت ناقص اسکرول کند.

### رفتار حذف مهارت

- حذف یک مهارت، تنظیمات level و years همان مهارت را نیز حذف می‌کند.
- اگر تنها مهارت موجود حذف شود، Next Disabled شود.
- پیام تأیید برای حذف معمول یک مهارت لازم نیست، چون عمل قابل بازگشت از مرحله قبل است.

### Wireframe مرحله چهارم

```text
┌──────────────────────────────────────────────────────────────┐
│ ✓ حوزه   ✓ گرایش   ✓ مهارت‌ها   ④ سطح و سابقه   ⑤ بازبینی │
├──────────────────────────────────────────────────────────────┤
│ سطح و سابقه هر مهارت را مشخص کنید                            │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Revit                         نرم‌افزاری · سازه       [×] │ │
│ │ سطح:  ( مبتدی ) (● متوسط) ( حرفه‌ای )                   │ │
│ │ سابقه: [ ۳ سال ▾ ]                                       │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ نظارت کارگاهی                   میدانی · سازه        [×] │ │
│ │ سطح:  ( مبتدی ) ( متوسط ) (● حرفه‌ای)                   │ │
│ │ سابقه: [ ۷ سال ▾ ]                                       │ │
│ └──────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│ [→ بازگشت]                                      [ادامه ←]   │
└──────────────────────────────────────────────────────────────┘
```

---

## 8. مرحله پنجم: بازبینی و ثبت

### هدف

نمایش یک Preview کامل و قابل بررسی پیش از اجرای ثبت نهایی جایگزین‌کننده.

### ساختار Preview

Preview در سه کارت خلاصه شود:

1. حوزه‌ها
2. گرایش‌ها
3. مهارت‌ها، سطح و سابقه

برای هر کارت یک لینک «ویرایش» وجود داشته باشد:

- ویرایش حوزه‌ها → مرحله ۱
- ویرایش گرایش‌ها → مرحله ۲
- ویرایش مهارت‌ها → مرحله ۳
- ویرایش سطح و سابقه → مرحله ۴

### جدول مهارت‌ها در دسکتاپ

| مهارت | نوع | گرایش | سطح | سابقه |
|---|---|---|---|---|
| Revit | نرم‌افزاری | سازه | متوسط | ۳ سال |
| نظارت کارگاهی | میدانی | سازه | حرفه‌ای | ۷ سال |

در موبایل، هر ردیف جدول به Card تبدیل شود تا اسکرول افقی لازم نباشد.

### هشدار ثبت

با توجه به رفتار `sync()`، متن کوتاه زیر بالای CTA نهایی نمایش داده شود:

> با ثبت نهایی، فهرست مهارت‌های فعلی حساب شما با انتخاب‌های این صفحه به‌روزرسانی می‌شود.

متن نباید فنی یا ترساننده باشد، اما باید ماهیت جایگزین‌شدن انتخاب‌ها را روشن کند.

### CTA نهایی

- دکمه اصلی: «ثبت مهارت‌ها»
- دکمه ثانویه: «بازگشت و ویرایش»
- هنگام ارسال، دکمه اصلی Disabled و دارای spinner شود.
- تا دریافت پاسخ، ارسال مجدد ممکن نباشد.
- در موفقیت، پیام موفقیت و redirect فعلی اجرا شود.
- در خطای validation یا شبکه، کاربر در مرحله پنجم بماند و state حفظ شود.

### Validation نهایی

پیش از فعال‌شدن CTA:

- ۱ تا ۲ حوزه
- ۱ تا ۲ گرایش در state UI
- ۱ تا ۵ مهارت
- level و years برای همه مهارت‌ها
- عدم وجود مهارت تکراری

Validation واقعی Backend همچنان مرجع نهایی است. خطای Backend باید بدون پاک‌شدن انتخاب‌ها نمایش داده شود.

### Wireframe مرحله پنجم

```text
┌────────────────────────────────────────────────────────────────┐
│ ✓ حوزه   ✓ گرایش   ✓ مهارت‌ها   ✓ سطح و سابقه   ⑤ بازبینی    │
├────────────────────────────────────────────────────────────────┤
│ بازبینی اطلاعات                                                │
│ پیش از ثبت، انتخاب‌های خود را بررسی کنید.                       │
│                                                                │
│ ┌ حوزه‌ها ─────────────────────────────────────── [ویرایش] ┐   │
│ │ مهندسی عمران · مهندسی مکانیک                              │   │
│ └────────────────────────────────────────────────────────────┘   │
│ ┌ گرایش‌ها ────────────────────────────────────── [ویرایش] ┐   │
│ │ سازه · طراحی کاربردی                                      │   │
│ └────────────────────────────────────────────────────────────┘   │
│ ┌ مهارت‌ها ────────────────────────────────────── [ویرایش] ┐   │
│ │ Revit             نرم‌افزاری   متوسط     ۳ سال            │   │
│ │ نظارت کارگاهی     میدانی       حرفه‌ای    ۷ سال            │   │
│ └────────────────────────────────────────────────────────────┘   │
│                                                                │
│ ℹ با ثبت نهایی، فهرست مهارت‌های حساب شما به‌روزرسانی می‌شود.  │
├────────────────────────────────────────────────────────────────┤
│ [→ بازگشت و ویرایش]                         [ثبت مهارت‌ها ✓]   │
└────────────────────────────────────────────────────────────────┘
```

---

## 9. رفتار Next و Back

### Next

- فقط مرحله فعلی را validation کند.
- در حالت نامعتبر Disabled باشد.
- پس از معتبرشدن مرحله، کاربر را به مرحله بعد ببرد.
- هیچ درخواست ثبت نهایی ایجاد نکند.
- در مرحله سوم، عبور فقط پس از انتخاب Skill انجام شود.
- در مرحله چهارم، عبور فقط پس از تکمیل level و years همه Skillها انجام شود.

### Back

- فقط شماره و visibility مرحله فعال را تغییر دهد.
- stateهای `selectedDomains`، `loadedSubdomainsByDomain`، `selectedSubdomains` و `selectedSkills` را reset نکند.
- کارت‌ها، سطح‌ها، سابقه‌ها و عبارت جست‌وجو را تا حد امکان حفظ کند.
- API مهارت را صرفاً به‌دلیل Back دوباره فراخوانی نکند.
- listenerها و کارت‌های مهارت را دوباره ایجاد نکند.
- بازگشت از Preview به یک مرحله، Preview را از state موجود بازسازی کند؛ نه از داده جدید یا state خالی.

### تغییر داده در مراحل قبلی

- افزودن داده سازگار، state مراحل بعدی را حفظ کند.
- حذف داده والد، فقط داده‌های وابسته را با تأیید کاربر حذف کند.
- پس از حذف داده وابسته، Stepper مرحله‌های بعدی را ناقص نشان دهد.
- کاربر نتواند از Stepper از روی مرحله ناقص عبور کند.

---

## 10. ماتریس وضعیت دکمه‌ها

| مرحله | Back | Next / Submit در حالت خالی | شرط فعال‌شدن |
|---|---|---|---|
| ۱. حوزه | مخفی یا Disabled | Disabled | حداقل ۱ و حداکثر ۲ حوزه |
| ۲. گرایش | Active | Disabled | حداقل ۱ و حداکثر ۲ گرایش معتبر |
| ۳. مهارت‌ها | Active | Disabled | حداقل ۱ و حداکثر ۵ مهارت |
| ۴. سطح و سابقه | Active | Disabled | level و years معتبر برای همه مهارت‌ها |
| ۵. بازبینی | Active | Disabled در ارسال | معتبر بودن کل state و ارسال‌نشدن درخواست جاری |

قواعد ظاهری Disabled:

- کاهش contrast بدون حذف کامل خوانایی
- cursor و حالت hover غیرفعال
- عدم تکیه صرف بر رنگ
- نمایش متن راهنمای کوتاه در نزدیکی دکمه، نه tooltip اجباری

---

## 11. رفتار Refresh

### رفتار پایه و سازگار با محدوده فعلی

چون state فعلی فقط در حافظه مرورگر نگهداری می‌شود:

- Refresh صفحه همه انتخاب‌های انجام‌نشده را پاک می‌کند.
- Wizard از مرحله اول و state خالی شروع می‌شود.
- هیچ وعده «ذخیره خودکار» یا «ادامه از جای قبلی» در UI نمایش داده نشود.

### هشدار خروج

برای طراحی پایه، Dialog اختصاصی مرورگر الزامی در نظر گرفته نمی‌شود؛ زیرا افزودن رفتار جدید نیازمند JavaScript است. اگر در فاز پیاده‌سازی UI-only مجاز شناخته شد، می‌توان فقط هنگام وجود تغییرات ثبت‌نشده از هشدار استاندارد خروج استفاده کرد.

### گزینه آینده

ذخیره Draft در `localStorage` می‌تواند در Sprint مستقل بررسی شود، اما جزء طراحی اجرایی اولیه نیست. این گزینه:

- Backend یا Database نمی‌خواهد.
- باید تاریخ انقضا و امکان «شروع مجدد» داشته باشد.
- باید UUIDها را پس از بازیابی با داده‌های موجود تطبیق دهد.
- نباید در این فاز فرض‌شده یا اجباری باشد.

---

## 12. Responsive دسکتاپ

### Layout

- Stepper افقی کامل
- Card اصلی با حداکثر عرض ۱۱۰۰ پیکسل
- فضای داخلی ۲۴ تا ۳۲ پیکسل
- Grid مهارت‌ها در ۲ یا ۳ ستون بر اساس عرض
- Preview مهارت‌ها به شکل جدول
- نوار Navigation در پایین Card

### رفتار محتوا

- Summary انتخاب‌ها می‌تواند در ستون کناری باریک یا نوار پایین محتوا قرار گیرد.
- در مرحله سوم، فهرست نرم‌افزاری و میدانی از نظر بصری جدا اما هم‌وزن باشند.
- CTA پیشنهاد Skill تمام‌عرض و در انتهای بخش مهارت‌های میدانی باشد.
- ارتفاع کارت‌ها در یک ردیف هماهنگ باشد.

### اندازه هدف‌های تعاملی

- حداقل ارتفاع دکمه‌ها ۴۴ پیکسل
- فاصله حداقل ۸ پیکسل بین کنترل‌های مجاور
- chipها دارای دکمه حذف مستقل و قابل کلیک باشند.

---

## 13. Responsive موبایل

### Layout

- Card اصلی بدون حاشیه سنگین یا تقریباً تمام‌عرض
- Padding افقی ۱۶ پیکسل
- Stepper فشرده با «مرحله X از ۵»
- همه فیلدها و CTAهای اصلی تمام‌عرض
- کارت مهارت تک‌ستونه
- Preview به‌صورت Card، نه جدول افقی

### Navigation

نوار پایین صفحه sticky باشد:

```text
┌────────────────────────────────┐
│ [بازگشت]          [ادامه ←]    │
└────────────────────────────────┘
```

- فضای امن پایین دستگاه رعایت شود.
- نوار روی آخرین محتوای صفحه نیفتد؛ پایین محتوا padding اضافه داشته باشد.
- در مرحله اول، Next تمام‌عرض باشد.
- در مرحله پنجم، «ثبت مهارت‌ها» بالاترین اولویت بصری را داشته باشد.

### مهارت‌ها

- گروه‌های نرم‌افزاری و میدانی Tab یا Accordion باشند.
- شمارنده انتخاب همیشه بالای فهرست قابل مشاهده باشد.
- Search در بالای کارت‌ها قرار گیرد.
- CTA پیشنهاد مهارت در انتهای Tab/Accordion میدانی نمایش داده شود.

### سطح و سابقه

- هر Skill یک Card عمودی
- levelها با Radio Cardهای بزرگ یا select
- years با select تمام‌عرض
- خطا دقیقاً زیر کنترل ناقص

### Wireframe موبایل

```text
┌──────────────────────────────┐
│ مرحله ۴ از ۵                 │
│ سطح و سابقه                  │
│ [████████████████░░░░]       │
├──────────────────────────────┤
│ Revit                        │
│ نرم‌افزاری · سازه            │
│                              │
│ سطح مهارت                    │
│ [مبتدی] [متوسط ✓] [حرفه‌ای] │
│                              │
│ سال تجربه                    │
│ [ ۳ سال                 ▾ ]  │
├──────────────────────────────┤
│ نظارت کارگاهی                │
│ میدانی · سازه                │
│ ...                          │
├──────────────────────────────┤
│ [بازگشت]        [ادامه ←]    │
└──────────────────────────────┘
```

---

## 14. نمایش Validation و خطا

### خطای محلی

- کنار همان dropdown، گروه یا Card نمایش داده شود.
- متن کوتاه، مستقیم و فارسی باشد.
- حاشیه کنترل نامعتبر تغییر کند.
- آیکون خطا در کنار متن استفاده شود.

### خطای مرحله

اگر بیش از یک خطا وجود دارد:

- خلاصه کوتاه در بالای مرحله نمایش داده شود.
- تمرکز به اولین کنترل نامعتبر منتقل شود.
- Stepper مرحله را دارای خطا نشان دهد.

### خطای Submit

- خطای Backend یا شبکه در مرحله پنجم نمایش داده شود.
- state و Preview پاک نشوند.
- دکمه پس از پایان درخواست دوباره فعال شود.
- خطای تطابق Skill و Domain به زبان قابل فهم نمایش داده شود:
  «یکی از مهارت‌ها با حوزه‌های انتخاب‌شده سازگار نیست. لطفاً مراحل قبل را بازبینی کنید.»
- برای خطای مرتبط با داده مرحله قبل، لینک «بازبینی انتخاب‌ها» ارائه شود.

---

## 15. حالت‌های Empty، Loading و Error

### Loading مهارت‌ها

- پس از انتخاب گرایش، به‌جای صفحه سفید Skeleton Card نمایش داده شود.
- در زمان Loading، انتخاب گرایش و Back قفل نشوند.
- Next تا وجود Skill انتخاب‌شده Disabled بماند.

### خطای بارگذاری Skill

- پیام Inline: «بارگذاری مهارت‌ها انجام نشد.»
- دکمه «تلاش دوباره»
- Skillهای قبلاً بارگذاری‌شده و انتخاب‌شده حفظ شوند.

### فهرست خالی

- Subdomain بدون Skill: «برای این گرایش هنوز مهارتی ثبت نشده است.»
- زیر Empty State مهارت میدانی، CTA پیشنهاد Skill نمایش داده شود.
- Empty State جست‌وجو با Empty State داده واقعی متفاوت باشد.

---

## 16. Accessibility

- Stepper دارای نام قابل فهم برای Screen Reader باشد.
- مرحله فعال با `aria-current="step"` مشخص شود.
- Disabled واقعی کنترل‌ها علاوه بر ظاهر اعمال شود.
- همه دکمه‌های icon-only دارای label متنی باشند.
- حذف chip با صفحه‌کلید قابل انجام باشد.
- Focus پس از Next به عنوان مرحله جدید منتقل شود.
- Focus پس از Back به عنوان مرحله قبلی منتقل شود.
- Modal پیشنهاد Skill پس از بسته‌شدن Focus را به CTA بازگرداند.
- رنگ تنها نشانه وضعیت انتخاب یا خطا نباشد؛ تیک، متن و حاشیه نیز استفاده شود.
- کنتراست متن و کنترل‌ها حداقل استاندارد WCAG AA را رعایت کند.
- انیمیشن انتقال مرحله کوتاه باشد و تنظیم `prefers-reduced-motion` را رعایت کند.

---

## 17. Microcopy پیشنهادی

### عنوان صفحه

«مهارت‌های تخصصی خود را ثبت کنید»

### توضیح بالای صفحه

«حوزه، گرایش و مهارت‌هایی را انتخاب کنید که در آن‌ها تجربه عملی دارید.»

### دکمه‌ها

- «ادامه»
- «بازگشت»
- «بازگشت و ویرایش»
- «ثبت مهارت‌ها»
- «پیشنهاد مهارت جدید»
- «تلاش دوباره»

### شمارنده‌ها

- «۱ از ۲ حوزه انتخاب شده»
- «۲ از ۲ گرایش انتخاب شده»
- «۳ از ۵ مهارت انتخاب شده»

### پیام موفقیت پیشنهاد Skill

«پیشنهاد شما برای بررسی ارسال شد. پس از تأیید مدیر به فهرست مهارت‌ها اضافه می‌شود.»

---

## 18. رفتار Preview پس از ویرایش

- کلیک «ویرایش» کاربر را به مرحله مرتبط ببرد.
- state سایر مراحل حفظ شود.
- پس از ویرایش، کاربر مجبور نباشد تمام مراحل سالم را از ابتدا پر کند.
- با این حال، برای رسیدن دوباره به Preview باید مراحل وابسته معتبر باشند.
- اگر تغییر حوزه باعث حذف گرایش یا Skill شده است، مراحل ۲ تا ۴ ناقص علامت بخورند.
- Preview هر بار از state جاری ساخته شود.
- اطلاعات Process در Preview نشان داده نشود؛ چون انتخاب مستقیم کاربر نیست.

---

## 19. مرزهای طراحی و مواردی که نباید تغییر کنند

- endpoint ثبت نهایی همان `/save-user-skills` باقی بماند.
- payload شامل `skills`, `skill_id`, `level`, `years`, `domains` باقی بماند.
- محدودیت ۲ Domain، ۲ Subdomain و ۵ Skill حفظ شود.
- دکمه ثبت فقط handler نهایی موجود را اجرا کند.
- پیشنهاد Skill فرم و submit مستقل خود را حفظ کند.
- Skillهای `software` و `field` هر دو از جدول `skills` هستند.
- هیچ جدول یا مفهوم UI جدیدی با عنوان `field_skills` فرض نشود.
- Process به مرحله مستقل تبدیل نشود و در payload UI قرار نگیرد.
- Back باعث فراخوانی دوباره API، duplicate شدن Card یا listener نشود.
- Wizard نباید ادعا کند انتخاب‌های قبلی حساب را بازیابی می‌کند؛ صفحه فعلی با state خالی آغاز می‌شود.
- Refresh در نسخه پایه state را پاک می‌کند.
- تغییر label نمایشی level نباید valueهای قراردادی فعلی را تغییر دهد.

---

## 20. معیارهای پذیرش طراحی

- Wizard دقیقاً پنج مرحله تعریف‌شده دارد.
- Wireframe هر پنج مرحله و نسخه موبایل موجود است.
- Stepper در دسکتاپ و موبایل تعریف شده است.
- Next فقط در صورت معتبر بودن مرحله فعال می‌شود.
- Back هیچ state معتبری را پاک نمی‌کند.
- حذف Domain یا Subdomain وابسته با هشدار روشن انجام می‌شود.
- حداکثر انتخاب‌های ۲، ۲ و ۵ در UI قابل مشاهده است.
- سطح و سابقه فقط در مرحله چهارم تکمیل می‌شوند.
- Preview شامل حوزه، گرایش، مهارت، نوع، سطح و سابقه است.
- پیشنهاد Skill در مرحله سوم و در بخش مهارت‌های میدانی قرار دارد.
- ثبت فقط در مرحله پنجم انجام می‌شود.
- خطای Submit باعث پاک‌شدن state نمی‌شود.
- رفتار Refresh به‌وضوح به‌عنوان شروع مجدد تعریف شده است.
- طراحی موبایل بدون اسکرول افقی قابل استفاده است.
- هیچ تغییر Backend یا Database برای اجرای نسخه پایه لازم نیست.

---

## جمع‌بندی

طراحی پیشنهادی، فرم فعلی را به یک جریان پنج‌مرحله‌ای روشن تبدیل می‌کند: کاربر ابتدا دامنه انتخاب‌های خود را با حوزه و گرایش محدود می‌کند، سپس Skillها را انتخاب می‌کند، سطح و سابقه را جداگانه تکمیل می‌کند و در پایان یک Preview کامل می‌بیند.

این جداسازی، شلوغی صفحه فعلی را کاهش می‌دهد و در عین حال قرارداد فنی موجود را حفظ می‌کند. مهم‌ترین اصل اجرایی آن است که مرحله‌بندی فقط لایه نمایش و navigation باشد؛ state، محدودیت‌ها، بارگذاری Skillها، payload و ثبت نهایی فعلی باید دست‌نخورده باقی بمانند.
