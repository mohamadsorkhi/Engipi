# سند طراحی UX/UI ویزارد ثبت پروژه EngiPi

## 1. هدف و اصول طراحی

هدف این طراحی، تبدیل فرم طولانی ثبت پروژه به یک تجربه مرحله‌ای، قابل‌فهم و کم‌خطا است؛ بدون تغییر در داده‌هایی که فرم نهایی ارسال می‌کند.

اصول اصلی:

- کاهش بار شناختی با نمایش فقط اطلاعات مرتبط با هر مرحله
- حفظ کامل اطلاعات هنگام حرکت بین مراحل
- جلوگیری از رسیدن خطاهای قابل‌پیشگیری به مرحله ثبت نهایی
- نمایش واضح وابستگی میان نوع همکاری، حوزه، پردازش و مهارت
- امکان بازبینی همه اطلاعات پیش از ثبت
- عملکرد مناسب در موبایل، تبلت و دسکتاپ
- استفاده از زبان فارسی ساده و لحن حرفه‌ای EngiPi
- عدم ایجاد حس «فرم طولانی پنهان‌شده»؛ هر مرحله باید هدف مشخص و حجم متعادل داشته باشد

---

## 2. تعداد مراحل و دلیل انتخاب

ویزارد در ۵ مرحله طراحی می‌شود:

1. معرفی پروژه
2. نوع همکاری و حوزه تخصصی
3. پردازش‌ها و سطح تخصص
4. مهارت‌ها، زمان و بودجه
5. فایل‌ها و بازبینی نهایی

### دلیل انتخاب پنج مرحله

- اطلاعات پایه باید پیش از جزئیات تخصصی دریافت شود.
- نوع همکاری روی فهرست مهارت‌های قابل نمایش اثر دارد.
- حوزه‌های انتخابی روی فهرست پردازش‌ها اثر می‌گذارند.
- پردازش‌ها اجباری‌اند و باید یک مرحله مستقل و متمرکز داشته باشند.
- مهارت‌ها اختیاری‌اند و بهتر است همراه شرایط اجرایی پروژه قرار گیرند.
- فایل‌ها اختیاری‌اند، اما بررسی امنیتی آنها ممکن است زمان‌بر باشد؛ قرارگیری در مرحله آخر انتظار کاربر را مدیریت می‌کند.
- Preview باید بلافاصله قبل از ثبت نهایی قرار گیرد تا کاربر بتواند خطاهای محتوایی را اصلاح کند.

پنج مرحله تعادل مناسبی میان کوتاه‌بودن هر صفحه و تعداد کلیک‌های لازم ایجاد می‌کند. تقسیم به مراحل بیشتر باعث پراکندگی و طولانی‌شدن مسیر می‌شود؛ مراحل کمتر نیز دوباره فرم‌های سنگین ایجاد می‌کند.

---

## 3. ساختار کلی صفحه

در تمام مراحل ساختار ثابت زیر حفظ می‌شود:

- عنوان صفحه: «ثبت پروژه مهندسی جدید»
- متن کوتاه راهنما
- Progress Stepper
- کارت اصلی مرحله
- خلاصه وضعیت ذخیره پیش‌نویس
- ناحیه عملیات شامل Back، Next یا Submit

متن بالای ویزارد:

«اطلاعات پروژه را مرحله‌به‌مرحله تکمیل کنید. تا پیش از ثبت نهایی می‌توانید به مراحل قبل بازگردید و اطلاعات را ویرایش کنید.»

### وضعیت‌های بصری هر مرحله

- آینده: خاکستری و غیرفعال
- جاری: آبی EngiPi با شماره برجسته
- تکمیل‌شده: سبز/فیروزه‌ای همراه علامت تیک
- دارای خطا: قرمز همراه نشان هشدار
- اختیاری: برچسب کوچک «اختیاری»

---

## 4. مرحله اول: معرفی پروژه

### هدف

دریافت تصویری روشن از ماهیت پروژه پیش از ورود به جزئیات تخصصی.

### فیلدها

1. عنوان پروژه
2. توضیحات فنی پروژه

### طراحی فیلد عنوان

- Label: «عنوان پروژه مهندسی»
- متن راهنما: «عنوانی کوتاه و مشخص بنویسید که خروجی اصلی پروژه را توضیح دهد.»
- مثال: «طراحی و پیاده‌سازی سیستم کنترل صنعتی»
- شمارنده تا سقف ۱۹۱ کاراکتر

### طراحی فیلد توضیحات

- Label: «توضیحات فنی پروژه»
- راهنمای ساختاری داخل یا زیر فیلد:
  - مسئله یا نیاز اصلی چیست؟
  - خروجی مورد انتظار چیست؟
  - چه استانداردها یا محدودیت‌هایی وجود دارد؟
- شمارنده زنده کاراکتر
- پیشنهاد UX: حداقل ۲۰ کاراکتر برای ادامه، مطابق تجربه فعلی فرم

### Validation مرحله

- عنوان خالی نباشد.
- عنوان بیشتر از ۱۹۱ کاراکتر نباشد.
- برای هماهنگی با تجربه فعلی، عنوان کمتر از ۵ کاراکتر پذیرفته نشود.
- توضیحات خالی نباشد.
- برای حفظ کیفیت پروژه، توضیحات کمتر از ۲۰ کاراکتر پذیرفته نشود.

### پیام‌های خطا

- «عنوان پروژه الزامی است.»
- «عنوان پروژه نباید بیشتر از ۱۹۱ کاراکتر باشد.»
- «عنوان پروژه باید حداقل ۵ کاراکتر باشد.»
- «توضیحات فنی پروژه الزامی است.»
- «توضیحات فنی پروژه باید حداقل ۲۰ کاراکتر باشد.»

### رفتار Next

- با کلیک روی «مرحله بعد»، فقط همین مرحله بررسی می‌شود.
- اولین فیلد نامعتبر focus می‌شود.
- در صورت معتبر بودن، مرحله تکمیل‌شده علامت می‌خورد و مرحله دوم نمایش داده می‌شود.

---

## 5. مرحله دوم: نوع همکاری و حوزه تخصصی

### هدف

تعیین نحوه اجرای پروژه و محدوده تخصصی آن؛ این انتخاب‌ها داده‌های مراحل بعد را کنترل می‌کنند.

### فیلدها

1. نوع همکاری
   - دورکاری
   - حضوری
   - ترکیبی
2. حوزه‌های تخصصی
   - حداقل ۱ حوزه
   - حداکثر ۳ حوزه
3. جست‌وجوی حوزه

### طراحی نوع همکاری

سه کارت انتخابی در یک ردیف دسکتاپ و به‌صورت عمودی در موبایل:

- آیکن
- عنوان
- توضیح کوتاه
- تیک واضح برای گزینه انتخاب‌شده

### طراحی حوزه‌ها

- کارت‌های حوزه همراه نام و checkbox
- نمایش شمارنده: «۲ از ۳ حوزه انتخاب شده»
- جست‌وجوی زنده
- در صورت رسیدن به سه حوزه، کارت‌های دیگر disable نشوند؛ کلیک روی آنها پیام واضح نمایش دهد تا کاربر متوجه محدودیت شود.
- حوزه‌های انتخاب‌شده حتی در حالت جست‌وجو همیشه قابل مشاهده باشند.

### Validation مرحله

- یک نوع همکاری انتخاب شده باشد.
- حداقل یک حوزه انتخاب شده باشد.
- بیشتر از سه حوزه انتخاب نشده باشد.

### پیام‌های خطا

- «نوع همکاری پروژه را انتخاب کنید.»
- «حداقل یک حوزه تخصصی انتخاب کنید.»
- «حداکثر سه حوزه تخصصی می‌توانید انتخاب کنید.»

### رفتار Next

- در صورت معتبر بودن، فهرست پردازش‌های مرحله سوم بر اساس حوزه‌ها آماده می‌شود.
- اگر حوزه‌ای انتخاب شده ولی هیچ پردازشی برای آن وجود ندارد، قبل از ورود به مرحله بعد پیام راهنما نمایش داده می‌شود:

  «برای حوزه انتخاب‌شده پردازشی تعریف نشده است. حوزه دیگری انتخاب کنید یا با پشتیبانی تماس بگیرید.»

---

## 6. مرحله سوم: پردازش‌ها و سطح تخصص

### هدف

مشخص‌کردن پردازش‌ها یا تخصص‌های عملی مورد نیاز پروژه و سطح مورد انتظار برای هرکدام.

### فیلدها

1. انتخاب پردازش
2. سطح مورد نیاز هر پردازش
   - عملی
   - مسلط
   - پیشرفته

### طراحی انتخاب پردازش

- فهرست Processها فقط از حوزه‌های مرحله قبل ساخته می‌شود.
- امکان جست‌وجو در پردازش‌ها وجود داشته باشد.
- حداکثر سه پردازش مطابق رفتار فعلی UI.
- پردازش‌های انتخاب‌شده به کارت مستقل تبدیل شوند.
- روی هر کارت:
  - نام پردازش
  - حوزه مربوط
  - انتخاب یک یا چند سطح
  - دکمه حذف

### Validation مرحله

- حداقل یک پردازش انتخاب شده باشد.
- حداکثر سه پردازش انتخاب شده باشد.
- برای هر پردازش حداقل یک سطح انتخاب شده باشد.
- هر پردازش باید متعلق به یکی از حوزه‌های انتخاب‌شده باشد.

### پیام‌های خطا

- «حداقل یک پردازش مورد نیاز پروژه را انتخاب کنید.»
- «حداکثر سه پردازش می‌توانید انتخاب کنید.»
- «برای ‹نام پردازش› حداقل یک سطح مورد نیاز انتخاب کنید.»
- «این پردازش با حوزه‌های انتخاب‌شده مطابقت ندارد.»

### رفتار Back

- بازگشت به مرحله دوم هیچ انتخابی را حذف نمی‌کند.
- اگر کاربر حوزه‌ها را تغییر ندهد، state پردازش‌ها کاملاً حفظ می‌شود.
- اگر حوزه‌ای حذف شود، پردازش‌های وابسته بلافاصله حذف نمی‌شوند؛ ابتدا هشدار وابستگی نمایش داده می‌شود.

---

## 7. مرحله چهارم: مهارت‌ها، زمان و بودجه

### هدف

تکمیل شرایط اجرایی و مهارت‌های اختیاری مورد نیاز پروژه.

### فیلدها

1. مهارت‌ها — اختیاری
2. سطح هر مهارت — در صورت انتخاب
3. سال‌های تجربه هر مهارت — در صورت انتخاب
4. مدت پروژه به روز — اختیاری
5. حداقل بودجه — اختیاری
6. حداکثر بودجه — اختیاری
7. مهلت تحویل — اختیاری و نیازمند تصمیم محصول

### طراحی مهارت‌ها

- عنوان section: «مهارت‌های تکمیلی مورد نیاز»
- توضیح: «در صورت نیاز، ابزارها یا مهارت‌های میدانی مشخص پروژه را انتخاب کنید.»
- فهرست مهارت‌ها مطابق Work Type فیلتر می‌شود.
- گروه‌بندی بر اساس حوزه و زیرحوزه حفظ می‌شود.
- انتخاب Skill اختیاری است.
- برای هر Skill انتخاب‌شده، کارت جزئیات نشان داده می‌شود:
  - نام Skill
  - سطح مورد نیاز
  - حداقل سابقه
  - حذف

### طراحی زمان و بودجه

- مدت پروژه و مهلت تحویل در یک زیرکارت «زمان‌بندی»
- حداقل و حداکثر بودجه در زیرکارت «بودجه پیشنهادی»
- واحد «تومان» کنار ورودی‌ها به‌صورت ثابت و واضح
- فرمت هزارگان هنگام تایپ
- متن راهنما: «در صورت نامشخص‌بودن بودجه، می‌توانید این بخش را خالی بگذارید.»

### تصمیم طراحی درباره Deadline

از آنجا که Backend و Database فیلد `deadline_date` را پشتیبانی می‌کنند ولی فرم اصلی فعلی آن را نمایش نمی‌دهد، در طراحی فضای آن پیش‌بینی می‌شود. فعال‌کردن واقعی این فیلد باید پس از تصمیم محصول انجام شود.

Label پیشنهادی:

«مهلت دریافت خروجی نهایی»

### Validation مرحله

اگر هیچ Skill انتخاب نشده باشد:

- بخش Skill معتبر است و کاربر می‌تواند ادامه دهد.

اگر Skill انتخاب شده باشد:

- شناسه Skill معتبر باشد.
- سطح خالی نباشد.
- سابقه عدد صحیح بین ۰ تا ۵۰ باشد.

زمان و بودجه:

- مدت پروژه در صورت ورود، عدد صحیح و حداقل ۱ روز باشد.
- حداقل بودجه در صورت ورود، عدد و غیرمنفی باشد.
- حداکثر بودجه در صورت ورود، عدد و غیرمنفی باشد.
- حداکثر بودجه کمتر از حداقل بودجه نباشد.
- Deadline در صورت فعال‌شدن، بعد از امروز باشد.

### پیام‌های خطا

- «سطح مورد نیاز برای ‹نام مهارت› را انتخاب کنید.»
- «سابقه مورد نیاز برای ‹نام مهارت› باید بین ۰ تا ۵۰ سال باشد.»
- «مدت پروژه باید حداقل یک روز باشد.»
- «حداقل بودجه نمی‌تواند منفی باشد.»
- «حداکثر بودجه باید بزرگ‌تر یا مساوی حداقل بودجه باشد.»
- «مهلت تحویل باید بعد از امروز باشد.»

---

## 8. مرحله پنجم: فایل‌ها و Preview نهایی

### هدف

افزودن مستندات اختیاری و بررسی کامل پروژه پیش از ثبت.

### بخش اول: Upload فایل

فیلد:

- فایل‌های پیوست — اختیاری و multiple

اطلاعات راهنما:

- حداکثر حجم هر فایل: ۱۰ مگابایت
- فرمت‌های مجاز:
  - PDF
  - TXT
  - CSV
  - DOC/DOCX
  - XLS/XLSX
  - ZIP
- فایل‌ها پیش از ثبت از نظر امنیتی بررسی می‌شوند.

### بخش دوم: Preview

Preview به شکل چند کارت خلاصه نمایش داده می‌شود:

1. معرفی پروژه
   - عنوان
   - توضیحات
2. نحوه اجرا و حوزه‌ها
   - Work Type
   - حوزه‌های انتخاب‌شده
3. پردازش‌ها
   - نام هر Process
   - سطح‌های انتخاب‌شده
4. مهارت‌ها
   - نام، سطح و سابقه
   - اگر خالی باشد: «مهارت تکمیلی مشخص نشده است.»
5. شرایط پروژه
   - مدت
   - مهلت تحویل، در صورت فعال‌بودن
   - بازه بودجه
6. فایل‌ها
   - نام
   - حجم
   - فرمت

هر کارت Preview یک لینک کوچک «ویرایش» دارد که کاربر را مستقیم به مرحله مرتبط می‌برد.

### CTA نهایی

- Primary: «ثبت نهایی پروژه»
- Secondary: «بازگشت و ویرایش»
- پیام اطمینان:

  «پس از ثبت، پروژه در بخش پروژه‌های من قابل مشاهده و مدیریت خواهد بود.»

### Validation مرحله

- فایل‌ها اختیاری‌اند.
- پیش از ارسال، حجم و extension فایل در UI بررسی و خطاهای واضح نمایش داده شود.
- نتیجه نهایی و قطعی MIME و اسکن امنیتی توسط سرور مشخص می‌شود.
- ثبت نهایی باید یک بار دیگر همه مراحل را از نظر state بررسی کند.

---

## 9. رفتار دکمه‌های Next و Back

### Next

- Label مراحل ۱ تا ۴: «مرحله بعد»
- در حال پردازش validation نباید spinner نمایش داده شود، چون درخواست شبکه‌ای وجود ندارد.
- فقط اطلاعات مرحله جاری validate می‌شوند.
- در صورت خطا، مرحله عوض نمی‌شود.
- اولین خطا focus شده و پیام آن نمایش داده می‌شود.
- در صورت موفقیت، بالای کارت مرحله بعد نمایش داده می‌شود.
- حرکت مرحله‌ای با transition کوتاه ۱۵۰ تا ۲۵۰ میلی‌ثانیه انجام شود.

### Back

- Label: «مرحله قبل»
- هیچ Validation اجرا نمی‌کند.
- هیچ داده‌ای را پاک نمی‌کند.
- کاربر دقیقاً به مرحله قبلی برمی‌گردد.
- موقعیت scroll به ابتدای کارت مرحله بازمی‌گردد.

### مرحله اول

- دکمه Back وجود ندارد.
- لینک «انصراف» در جای ثانویه قرار دارد.

### مرحله پنجم

- Next با «ثبت نهایی پروژه» جایگزین می‌شود.
- هنگام Submit:
  - دکمه disable می‌شود.
  - spinner نشان داده می‌شود.
  - متن به «در حال ثبت و بررسی فایل‌ها…» تغییر می‌کند.
  - کلیک مجدد یا submit تکراری مسدود می‌شود.

### انصراف

اگر اطلاعاتی وارد شده باشد:

«اطلاعات واردشده هنوز ثبت نشده است. آیا می‌خواهید از فرم خارج شوید؟»

گزینه‌ها:

- «ادامه تکمیل فرم»
- «خروج از فرم»

اگر Draft فعال باشد، متن باید بگوید پیش‌نویس حفظ خواهد شد.

---

## 10. Progress Stepper

### دسکتاپ

Stepper افقی با پنج node:

1. معرفی
2. حوزه
3. پردازش‌ها
4. شرایط
5. بازبینی

هر node شامل:

- شماره یا تیک
- عنوان کوتاه
- خط اتصال

رفتار:

- مراحل تکمیل‌شده قابل کلیک‌اند.
- مرحله جاری قابل کلیک نیست.
- مراحل آینده تا تکمیل معتبر مراحل قبل غیرفعال‌اند.
- پس از رسیدن کاربر به یک مرحله، مراجعه مستقیم به آن مجاز می‌شود؛ مشروط به اینکه تغییر مراحل قبلی dependency آن را نامعتبر نکرده باشد.

### موبایل

Stepper کامل افقی جای خود را به ساختار فشرده می‌دهد:

- متن «مرحله ۲ از ۵»
- عنوان مرحله
- progress bar خطی
- در صورت فضای کافی، پنج نقطه کوچک بدون label

این روش از شکستن عنوان‌ها و scroll افقی جلوگیری می‌کند.

### وضعیت خطا

اگر Submit نهایی خطای Backend برگرداند:

- step مربوط نشان قرمز می‌گیرد.
- با کلیک یا انتقال خودکار، مرحله خطادار باز می‌شود.
- تکمیل‌شده‌بودن آن مرحله تا رفع خطا برداشته می‌شود.

---

## 11. رفتار در موبایل

Breakpoint هدف: کمتر از ۷۶۸px.

### Layout

- کارت مرحله تمام عرض
- padding کمتر
- فیلدها تک‌ستونه
- کارت‌های Work Type عمودی
- کارت‌های Domain دو ستونه در عرض‌های متوسط و تک‌ستونه در موبایل کوچک
- کارت Process و Skill تک‌ستونه
- Preview به صورت accordion یا کارت‌های پشت‌سرهم

### Navigation

- نوار عملیات پایین صفحه sticky باشد.
- Next تمام عرض یا بخش بزرگ‌تر نوار را بگیرد.
- Back به‌صورت دکمه secondary کوچک‌تر کنار آن قرار گیرد.
- نوار sticky نباید روی آخرین فیلد یا پیام خطا بیفتد؛ فضای انتهایی کافی لازم است.

### Keyboard

- با بازشدن کیبورد موبایل، نوار sticky مزاحم فیلد نباشد.
- focus روی خطا باید فیلد را بالاتر از کیبورد قرار دهد.
- ورودی‌های عددی از keyboard عددی مناسب استفاده کنند.

### Search و انتخاب

- dropdownهای Choices داخل viewport باز شوند.
- ارتفاع فهرست محدود و قابل scroll باشد.
- targetهای لمسی حداقل ۴۴px باشند.

### Preview

- هر section به صورت accordion باز و بسته شود.
- section دارای خطا به صورت پیش‌فرض باز باشد.

---

## 12. رفتار در دسکتاپ

### Layout پیشنهادی

- عرض اصلی محتوا: حدود ۹۶۰ تا ۱۱۲۰px
- Stepper در بالای کارت
- فرم در ستون اصلی
- یک sidebar خلاصه اختیاری در مراحل ۲ تا ۴:
  - عنوان پروژه
  - نوع همکاری
  - تعداد حوزه
  - تعداد Process
  - تعداد Skill

Sidebar نباید input تعاملی داشته باشد؛ فقط خلاصه است تا دو منبع state ایجاد نشود.

### Grid

- عنوان و توضیحات تمام عرض
- Work Type سه ستون
- Domainها تا شش ستون بر اساس عرض
- Processها و Skillها دو یا سه ستون
- مدت و بودجه سه ستون
- Preview دو ستون در صفحه‌های بزرگ

### Navigation

- Back سمت راست در RTL
- Next سمت چپ یا انتهای مسیر بصری عملیات
- متن «مرحله بعد: حوزه و نوع همکاری» می‌تواند کنار Next به کاربر جهت بدهد.

---

## 13. محل نمایش خطاها

خطاها در چهار سطح نمایش داده می‌شوند:

### 13.1 کنار فیلد

- border قرمز
- متن خطا دقیقاً زیر فیلد
- آیکن هشدار کوچک
- متن خطا نباید فقط با رنگ مشخص شود.

### 13.2 خطای گروه

برای Domain، Process و Skill:

- پیام در ابتدای یا انتهای همان section
- کارت یا container گروه border قرمز می‌گیرد.

### 13.3 خلاصه خطاهای مرحله

اگر بیش از یک خطا وجود داشته باشد، بالای کارت مرحله یک alert نمایش داده شود:

«برای ادامه، ۳ مورد مشخص‌شده را اصلاح کنید.»

فهرست خطاها قابل کلیک باشد و هر مورد به فیلد مربوط scroll کند.

### 13.4 خطای Submit نهایی

- خطاهای Backend به step مربوط map شوند.
- ابتدا اولین step خطادار باز شود.
- Stepper نشان خطا بگیرد.
- toast فقط یک پیام عمومی نشان دهد:

  «برخی اطلاعات نیاز به اصلاح دارند.»

- متن دقیق خطا باید داخل فرم نمایش داده شود، نه فقط toast.

### خطای عمومی یا شبکه

در Preview باقی بماند و alert ثابت نمایش داده شود:

«ارتباط با سرور برقرار نشد. اطلاعات شما در فرم باقی مانده است؛ دوباره تلاش کنید.»

---

## 14. رفتار هنگام Refresh صفحه

### بدون Draft

رفتار browser به‌تنهایی قابل اتکا نیست. Refresh ممکن است تمام state را از بین ببرد، به‌خصوص:

- انتخاب‌های Choices
- کارت‌های Process و Skill
- فایل‌های انتخابی

اگر Draft خودکار هنوز فعال نشده باشد، هنگام Refresh یا خروج از صفحه و وجود داده:

«با تازه‌سازی صفحه ممکن است اطلاعات واردشده از بین برود.»

### با Draft محلی

پس از Refresh:

1. وجود Draft بررسی شود.
2. یک prompt غیرمزاحم نمایش داده شود:

   «یک پیش‌نویس ذخیره‌شده از این پروژه پیدا شد.»

3. گزینه‌ها:
   - «ادامه پیش‌نویس»
   - «شروع فرم جدید»
4. پس از بازیابی:
   - کاربر به آخرین مرحله معتبر بازگردد.
   - فیلدها و انتخاب‌ها restore شوند.
   - پیام «پیش‌نویس بازیابی شد» نمایش داده شود.

### محدودیت فایل پس از Refresh

مرورگر اجازه بازیابی خودکار فایل واقعی را نمی‌دهد. بنابراین:

- نام فایل‌ها می‌تواند در Draft به‌عنوان یادآوری نمایش داده شود.
- فایل‌ها باید دوباره انتخاب شوند.
- پیام واضح:

  «به دلایل امنیتی، فایل‌های پیوست بازیابی نشدند. لطفاً دوباره آنها را انتخاب کنید.»

---

## 15. رفتار هنگام تغییر Work Type

Work Type روی Skillهای مرحله چهارم اثر دارد:

- دورکاری: Skillهای software
- حضوری: Skillهای field
- ترکیبی: همه Skillها

### اگر هنوز Skill انتخاب نشده باشد

- Work Type بلافاصله تغییر می‌کند.
- در ورود بعدی به مرحله چهارم، فهرست جدید نمایش داده می‌شود.

### اگر Skill انتخاب شده و همه با Work Type جدید سازگار باشند

- تغییر بدون هشدار انجام می‌شود.
- انتخاب‌ها حفظ می‌شوند.

### اگر Skill ناسازگار وجود داشته باشد

پیش از تغییر modal تأیید نمایش داده شود:

«با تغییر نوع همکاری به ‹حضوری›، ۲ مهارت نرم‌افزاری انتخاب‌شده حذف می‌شوند.»

گزینه‌ها:

- «تغییر و حذف مهارت‌های ناسازگار»
- «انصراف»

فهرست مهارت‌های حذف‌شونده داخل modal نمایش داده شود.

### پس از تأیید

- فقط Skillهای ناسازگار حذف شوند.
- toast:

  «نوع همکاری تغییر کرد و ۲ مهارت ناسازگار حذف شد.»

### اثر روی Preview و Stepper

- Preview فوراً به‌روزرسانی شود.
- اگر مرحله چهارم قبلاً تکمیل شده و Skillهای حذف‌شده باعث ناقص‌شدن کارت باقی‌مانده شوند، وضعیت تکمیل مرحله دوباره بررسی شود.

---

## 16. رفتار هنگام تغییر حوزه‌های قبلی

این رفتار برای جلوگیری از Bug حیاتی است.

### افزودن حوزه

- Processهای فعلی حفظ می‌شوند.
- Processهای حوزه جدید به فهرست مرحله سوم اضافه می‌شوند.

### حذف حوزه بدون Process وابسته

- بدون هشدار حذف می‌شود.

### حذف حوزه دارای Process انتخاب‌شده

modal:

«با حذف حوزه ‹مهندسی مکانیک›، پردازش‌های زیر نیز از پروژه حذف می‌شوند: …»

گزینه‌ها:

- «حذف حوزه و پردازش‌های وابسته»
- «انصراف»

### Skillهای مرتبط

در معماری فعلی Skillها در Backend با Domain cross-check نمی‌شوند، اما در طراحی UX بهتر است حذف حوزه صرفاً Skillها را خودکار حذف نکند؛ چون Work Type مبنای اصلی فیلتر فعلی Skill است. در Preview می‌توان ناسازگاری احتمالی را به‌عنوان هشدار غیرمسدودکننده نمایش داد تا زمانی که قانون محصول مشخص شود.

---

## 17. رفتار هنگام برگشت به مراحل قبلی

- همه مقادیر باید حفظ شوند.
- انتخاب‌های Choices دوباره initialize نشوند.
- scroll هر مرحله می‌تواند آخرین موقعیت همان مرحله را حفظ کند؛ در نسخه ساده‌تر، ابتدای مرحله نمایش داده شود.
- Back هیچ Validation اجباری ندارد.
- کاربر می‌تواند مقدار معتبر قبلی را نامعتبر کند؛ در این حالت:
  - تیک تکمیل آن مرحله حذف می‌شود.
  - مراحل وابسته ممکن است وضعیت «نیازمند بازبینی» بگیرند.
- پس از ویرایش مرحله قبل، Next دوباره Validation همان مرحله را اجرا می‌کند.
- رفتن مستقیم از Stepper به یک مرحله تکمیل‌شده مجاز است.
- رفتن مستقیم به مراحل آینده‌ای که هنوز دیده یا معتبر نشده‌اند مجاز نیست.

---

## 18. طراحی Preview قبل از ثبت

### هدف Preview

Preview نباید فقط تکرار خام فیلدها باشد. باید خلاصه‌ای قابل اسکن و مشابه کارت پروژه نهایی ارائه کند.

### ساختار

- عنوان بزرگ
- Badge نوع همکاری
- حوزه‌ها به‌صورت chip
- توضیحات با line break و typography فارسی
- Processها به‌صورت کارت با levelها
- Skillها به‌صورت chip/card با سطح و سابقه
- بودجه با فرمت تومان
- مدت به روز
- Deadline در صورت وجود
- فایل‌ها با آیکن فرمت و حجم

### حالت داده خالی

برای فیلدهای اختیاری به‌جای خط تیره:

- «بودجه مشخص نشده»
- «مدت پروژه مشخص نشده»
- «مهارت تکمیلی انتخاب نشده»
- «فایل پیوست نشده»

### ویرایش از Preview

کنار هر section لینک «ویرایش»:

- معرفی → مرحله ۱
- نوع همکاری/حوزه → مرحله ۲
- Process → مرحله ۳
- Skill/شرایط → مرحله ۴
- فایل → همان مرحله ۵

پس از ویرایش، کاربر باید بتواند با گزینه «بازگشت به بازبینی» مستقیم به Preview برگردد؛ مشروط به معتبر بودن مرحله و مراحل وابسته.

### تأیید نهایی

نیازی به checkbox اجباری جدید نیست، مگر تصمیم محصول. افزودن checkbox صرفاً اصطکاک ایجاد می‌کند و Backend فعلی نیز چنین داده‌ای ندارد.

---

## 19. رفتار Upload فایل

### انتخاب فایل

- کل ناحیه Upload قابل کلیک باشد.
- Drag & Drop در دسکتاپ
- دکمه «انتخاب فایل» در موبایل
- امکان انتخاب چند فایل

### هر فایل در صف

- نام فایل
- extension
- حجم
- وضعیت
- دکمه حذف

وضعیت‌های پیشنهادی:

- آماده ارسال
- نامعتبر از نظر حجم
- نامعتبر از نظر فرمت
- در حال بررسی
- ردشده توسط سرور

### Validation اولیه

قبل از Submit:

- بیشتر از ۱۰MB → رد فوری
- extension خارج از فهرست → رد فوری

اما متن UI باید روشن کند:

«تأیید نهایی نوع و امنیت فایل هنگام ثبت پروژه انجام می‌شود.»

### هنگام Submit

- همه فایل‌ها در وضعیت «در حال بررسی امنیتی» قرار گیرند.
- دکمه ثبت غیرفعال باشد.
- خروج از صفحه با هشدار انجام شود.
- اگر یک فایل رد شود، کل ثبت انجام نمی‌شود و کاربر در مرحله پنجم باقی می‌ماند.
- فایل خطادار برجسته شود و پیام Server کنار همان فایل نمایش داده شود.
- فایل‌های دیگر در UI باقی بمانند.

### حذف و جایگزینی

- حذف فایل بدون تأیید اضافه، مگر فایل بزرگ یا upload شروع‌شده باشد.
- جایگزینی با انتخاب مجدد ممکن باشد.

---

## 20. طراحی Draft خودکار

این بخش صرفاً پیشنهاد طراحی است و تصمیم فنی/پیاده‌سازی جداگانه نیاز دارد.

### هدف

- جلوگیری از از دست رفتن اطلاعات در فرم طولانی
- امکان ادامه کار پس از بستن tab یا قطع اینترنت
- کاهش اضطراب کاربر

### داده‌های قابل ذخیره

- عنوان
- توضیحات
- Work Type
- Domainها
- Processها و levelها
- Skillها، level و years
- مدت
- بودجه
- Deadline در صورت فعال‌بودن
- شماره آخرین مرحله معتبر
- زمان آخرین ذخیره
- نام و metadata فایل‌ها، نه محتوای فایل

### زمان ذخیره

- پس از توقف کوتاه کاربر در ورودی متنی
- بلافاصله بعد از انتخاب‌های ساختاری
- هنگام Next و Back
- پیش از خروج از صفحه

### وضعیت بصری

متن کوچک زیر Stepper:

- «در حال ذخیره پیش‌نویس…»
- «پیش‌نویس ذخیره شد — ۱۴:۳۲»
- «ذخیره پیش‌نویس انجام نشد»

### مدت نگهداری

پیشنهاد UX: ۷ روز.

پس از آن:

«این پیش‌نویس قدیمی است. آیا می‌خواهید آن را ادامه دهید؟»

### شروع فرم جدید

اگر Draft وجود داشته باشد:

- ادامه پیش‌نویس
- مشاهده خلاصه پیش‌نویس
- حذف و شروع جدید

حذف Draft نیازمند تأیید است.

### Draft محلی در برابر Draft سرور

#### Draft محلی

مزایا:

- بدون تغییر Backend
- سریع
- مناسب فاز اول

محدودیت‌ها:

- فقط همان مرورگر/دستگاه
- پاک‌شدن با پاک‌سازی storage
- فایل‌ها قابل بازیابی نیستند

#### Draft سرور

مزایا:

- ادامه روی دستگاه دیگر
- پایداری بیشتر

محدودیت‌ها:

- نیازمند طراحی Backend و Database
- نیازمند مدیریت مالکیت، انقضا و امنیت

پیشنهاد محصول:

- فاز اول: Draft محلی
- فاز بعد: Draft سرور، فقط اگر داده‌های تحلیلی نشان دهد کاربران واقعاً فرم را نیمه‌کاره رها و بعداً ادامه می‌دهند.

### پس از ثبت موفق

- Draft فوراً حذف شود.
- در صورت redirect ناموفق، وضعیت «ثبت شده» مانع ارسال دوباره Draft شود.

---

## 21. Accessibility و جزئیات تعاملی

- کل رابط `dir=rtl` و فارسی
- label واقعی برای همه فیلدها
- وضعیت‌ها فقط با رنگ نمایش داده نشوند.
- Stepper برای screen reader وضعیت جاری را اعلام کند.
- پیام خطا به فیلد مرتبط باشد.
- focus هنگام تغییر مرحله روی عنوان مرحله قرار گیرد.
- modalهای dependency focus trap و عنوان واضح داشته باشند.
- کارت‌های انتخابی با keyboard قابل انتخاب باشند.
- motion در صورت ترجیح reduced motion کاربر حذف شود.
- contrast رنگ‌های خاکستری، آبی، سبز و قرمز مطابق استاندارد خوانایی باشد.

---

## 22. Microcopy پیشنهادی

### بالای فرم

«ثبت پروژه حدود ۵ دقیقه زمان می‌برد. اطلاعات شما تا پیش از ثبت نهایی قابل ویرایش است.»

### مرحله پردازش

«پردازش‌هایی را انتخاب کنید که متخصص باید برای اجرای این پروژه انجام دهد.»

### مرحله Skill

«مهارت‌های تکمیلی اختیاری‌اند؛ فقط مواردی را انتخاب کنید که برای پروژه ضروری هستند.»

### Preview

«اطلاعات پروژه را یک بار بررسی کنید. پس از ثبت نیز می‌توانید جزئیات پروژه را از پنل خود مدیریت کنید.»

### Upload

«فایل‌ها خصوصی نگهداری می‌شوند و فقط کاربران مجاز به آنها دسترسی خواهند داشت.»

---

# Wireframeهای متنی

## Wireframe مرحله ۱ — معرفی پروژه

```text
┌──────────────────────────────────────────────────────────────┐
│ ثبت پروژه مهندسی جدید                                      │
│ مرحله‌به‌مرحله اطلاعات پروژه را تکمیل کنید.                 │
├──────────────────────────────────────────────────────────────┤
│  [● معرفی] ─ [۲ حوزه] ─ [۳ پردازش] ─ [۴ شرایط] ─ [۵ بازبینی]│
│  مرحله ۱ از ۵  ████░░░░░░░░░░░░░░░░                        │
├──────────────────────────────────────────────────────────────┤
│ معرفی پروژه                                                 │
│                                                              │
│ عنوان پروژه مهندسی *                                        │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ طراحی و پیاده‌سازی سیستم کنترل صنعتی                   │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                             ۳۹ / ۱۹۱         │
│                                                              │
│ توضیحات فنی پروژه *                                          │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ مسئله، خروجی مورد انتظار، استانداردها و محدودیت‌ها...  │ │
│ │                                                          │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                              ۱۲۰ کاراکتر     │
├──────────────────────────────────────────────────────────────┤
│ انصراف                                      [مرحله بعد ←]    │
└──────────────────────────────────────────────────────────────┘
```

## Wireframe مرحله ۲ — نوع همکاری و حوزه

```text
┌──────────────────────────────────────────────────────────────┐
│  [✓ معرفی] ─ [● حوزه] ─ [۳ پردازش] ─ [۴ شرایط] ─ [۵ بازبینی]│
│  مرحله ۲ از ۵  ████████░░░░░░░░░░░░                        │
├──────────────────────────────────────────────────────────────┤
│ نحوه اجرای پروژه *                                           │
│                                                              │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐           │
│ │   دورکاری    │ │    حضوری     │ │    ترکیبی ✓ │           │
│ │ از راه دور   │ │ در محل کار   │ │ حضوری/آنلاین│           │
│ └──────────────┘ └──────────────┘ └──────────────┘           │
│                                                              │
│ حوزه‌های تخصصی *                              ۲ از ۳          │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 🔍 جست‌وجو در حوزه‌ها...                               │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ [✓ مکانیک] [✓ برق] [ عمران ] [ معماری ] [ صنایع ] [ مواد ] │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│ [→ مرحله قبل]                              [مرحله بعد ←]     │
└──────────────────────────────────────────────────────────────┘
```

## Wireframe مرحله ۳ — پردازش‌ها

```text
┌──────────────────────────────────────────────────────────────┐
│  [✓ معرفی] ─ [✓ حوزه] ─ [● پردازش] ─ [۴ شرایط] ─ [۵ بازبینی]│
│  مرحله ۳ از ۵  ████████████░░░░░░░░                        │
├──────────────────────────────────────────────────────────────┤
│ پردازش‌های مورد نیاز *                        ۲ از ۳          │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 🔍 جست‌وجوی پردازش...                                  │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ [ طراحی سیستم ] [ تحلیل تنش ] [ نقشه‌کشی ] [ شبیه‌سازی ]   │
│                                                              │
│ ┌─────────────────────────┐ ┌─────────────────────────┐      │
│ │ طراحی سیستم        [×] │ │ تحلیل تنش          [×] │      │
│ │ حوزه: مهندسی مکانیک    │ │ حوزه: مهندسی مکانیک    │      │
│ │ سطح مورد نیاز:          │ │ سطح مورد نیاز:          │      │
│ │ [✓ عملی] [✓ مسلط] [ ]  │ │ [ ] عملی [✓ مسلط] [ ]  │      │
│ └─────────────────────────┘ └─────────────────────────┘      │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│ [→ مرحله قبل]                              [مرحله بعد ←]     │
└──────────────────────────────────────────────────────────────┘
```

## Wireframe مرحله ۴ — مهارت‌ها، زمان و بودجه

```text
┌──────────────────────────────────────────────────────────────┐
│  [✓ معرفی] ─ [✓ حوزه] ─ [✓ پردازش] ─ [● شرایط] ─ [۵ بازبینی]│
│  مرحله ۴ از ۵  ████████████████░░░░                        │
├──────────────────────────────────────────────────────────────┤
│ مهارت‌های تکمیلی (اختیاری)                                  │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 🔍 جست‌وجوی مهارت...                                   │ │
│ └──────────────────────────────────────────────────────────┘ │
│ [ AutoCAD ] [ Revit ] [ بازرسی فنی ] [ نصب تجهیزات ]       │
│                                                              │
│ ┌─────────────────────────┐                                  │
│ │ AutoCAD            [×] │                                  │
│ │ سطح: [مسلط ▼]           │                                  │
│ │ سابقه: [ ۳ ] سال        │                                  │
│ └─────────────────────────┘                                  │
│                                                              │
│ زمان‌بندی                                                    │
│ مدت پروژه (روز) [ ۳۰ ]       مهلت تحویل [ ۱۴۰۵/۰۲/۱۰ ]      │
│                                                              │
│ بودجه پیشنهادی                                               │
│ حداقل [ ۵,۰۰۰,۰۰۰ ] تومان   حداکثر [ ۱۰,۰۰۰,۰۰۰ ] تومان    │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│ [→ مرحله قبل]                              [مرحله بعد ←]     │
└──────────────────────────────────────────────────────────────┘
```

## Wireframe مرحله ۵ — فایل و Preview

```text
┌──────────────────────────────────────────────────────────────┐
│  [✓ معرفی] ─ [✓ حوزه] ─ [✓ پردازش] ─ [✓ شرایط] ─ [● بازبینی]│
│  مرحله ۵ از ۵  ████████████████████                         │
├──────────────────────────────────────────────────────────────┤
│ فایل‌های پیوست (اختیاری)                                    │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │        فایل‌ها را اینجا رها کنید                         │ │
│ │              [انتخاب فایل]                              │ │
│ │ PDF, DOCX, XLSX, ZIP — حداکثر ۱۰MB                      │ │
│ └──────────────────────────────────────────────────────────┘ │
│ [📄 specification.pdf | ۲.۴MB | آماده ارسال | ×]            │
│                                                              │
│ بازبینی نهایی                                                │
│ ┌─────────────────────────┐ ┌─────────────────────────┐      │
│ │ معرفی پروژه      ویرایش│ │ نوع همکاری/حوزه  ویرایش│      │
│ │ طراحی سیستم کنترل...   │ │ ترکیبی                   │      │
│ │ توضیحات فنی پروژه...   │ │ مکانیک، برق              │      │
│ └─────────────────────────┘ └─────────────────────────┘      │
│ ┌─────────────────────────┐ ┌─────────────────────────┐      │
│ │ پردازش‌ها        ویرایش│ │ شرایط پروژه       ویرایش│      │
│ │ طراحی: عملی، مسلط      │ │ ۳۰ روز                    │      │
│ │ تحلیل: مسلط            │ │ ۵ تا ۱۰ میلیون تومان     │      │
│ └─────────────────────────┘ └─────────────────────────┘      │
│                                                              │
│ اطلاعات را بررسی کنید؛ فایل‌ها هنگام ثبت اسکن می‌شوند.       │
├──────────────────────────────────────────────────────────────┤
│ [→ بازگشت و ویرایش]                     [ثبت نهایی پروژه]    │
└──────────────────────────────────────────────────────────────┘
```

## Wireframe فشرده موبایل

```text
┌──────────────────────────┐
│ مرحله ۳ از ۵             │
│ پردازش‌های مورد نیاز     │
│ ████████████░░░░░░░░    │
├──────────────────────────┤
│ 🔍 جست‌وجوی پردازش...   │
│                          │
│ [ طراحی سیستم ]          │
│ [ تحلیل تنش ]            │
│ [ نقشه‌کشی ]             │
│                          │
│ ┌──────────────────────┐ │
│ │ طراحی سیستم      [×]│ │
│ │ مکانیک              │ │
│ │ [✓ عملی] [✓ مسلط]  │ │
│ └──────────────────────┘ │
│                          │
│                          │
├──────────────────────────┤
│ [مرحله قبل] [مرحله بعد] │  ← نوار sticky
└──────────────────────────┘
```

---

## 23. معیارهای پذیرش طراحی

طراحی زمانی موفق تلقی می‌شود که:

- کاربر در هر لحظه بداند در کدام مرحله قرار دارد.
- امکان ورود به مرحله بعد بدون تکمیل الزامات مرحله جاری وجود نداشته باشد.
- Back هیچ اطلاعاتی را حذف نکند.
- تغییر Work Type یا Domain بدون هشدار باعث حذف داده وابسته نشود.
- خطای Backend همیشه کاربر را به مرحله و فیلد درست هدایت کند.
- Preview تمام داده‌های نهایی را به زبان قابل‌فهم نمایش دهد.
- فایل انتخابی هنگام جابه‌جایی مرحله حفظ شود.
- Refresh بدون Draft با هشدار و با Draft با امکان بازیابی همراه باشد.
- موبایل بدون scroll افقی و با targetهای لمسی مناسب قابل استفاده باشد.
- ثبت نهایی فقط یک بار ارسال شود و وضعیت اسکن فایل برای کاربر روشن باشد.

